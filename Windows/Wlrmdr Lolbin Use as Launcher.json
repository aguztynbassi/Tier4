[
  {
    "name": "Wlrmdr Lolbin Use as Launcher",
    "description": "Detects the use of wlrmdr.exe to proxy launch other executables, a technique abused by attackers.",
    "tags": [
      "security",
      "detection",
      "defense evasion",
      "indirect command execution",
      "TA0005",
      "T1202",
      "Windows",
      "wlrmdr",
      "lolbin"
    ],
    "prompts": [
      {
        "id": "1",
        "prompt": "MEDIUM-SEVERITY ALERT: Suspicious use of `wlrmdr.exe` (Windows Logon Reminder) as a launcher detected on host '{host}' by user '{user}'. The process '{process_name}' at '{process_path}' was executed with command line '{command_line}', and its parent process '{parent_process_name}' is not the legitimate `winlogon.exe`. This indicates an attempt to leverage a LOLBIN for indirect command execution or defense evasion. Event Details:\n\n* **Timestamp**: {timestamp}\n* **Event ID**: {event_id}\n* **Process Name**: {process_name}\n* **Process Path**: {process_path}\n* **Command Line**: {command_line}\n* **Parent Process Name**: {parent_process_name}\n* **Parent Process Path**: {parent_process_path}\n* **User**: {user}\n* **Host**: {host}\n\n**RECOMMENDED ACTION:** Investigate the context of this execution. Determine if `wlrmdr.exe` was intended to be launched by `{parent_process_name}`. Analyze the full command line of `wlrmdr.exe` for any suspicious arguments or paths to executables. Examine the reputation and purpose of the parent process and any subsequent processes launched. Look for associated network connections, file modifications, or other abnormal system behavior. If deemed malicious, conduct a thorough forensic analysis to determine the initial compromise vector and any persistence mechanisms. This alert may require further tuning or baselining to reduce false positives in your environment.",
        "continueOnError": false,
        "systemCapability": "analyzeSecurityEvent"
      }
    ],
    "parameters": [
      {
        "name": "timestamp",
        "required": true,
        "description": "The timestamp of the process creation event."
      },
      {
        "name": "event_id",
        "required": true,
        "description": "The Windows Security Event ID for Process Creation (4688)."
      },
      {
        "name": "process_name",
        "required": true,
        "description": "The name of the process that was executed, which should be 'wlrmdr.exe'."
      },
      {
        "name": "process_path",
        "required": true,
        "description": "The full path to the wlrmdr.exe executable."
      },
      {
        "name": "command_line",
        "required": true,
        "description": "The full command line used to execute wlrmdr.exe, including suspicious flags."
      },
      {
        "name": "user",
        "required": true,
        "description": "The user account under which wlrmdr.exe was executed."
      },
      {
        "name": "host",
        "required": true,
        "description": "The hostname where the event occurred."
      },
      {
        "name": "parent_process_name",
        "required": true,
        "description": "The name of the parent process that launched wlrmdr.exe (should not be winlogon.exe)."
      },
      {
        "name": "parent_process_path",
        "required": true,
        "description": "The full path to the parent process."
      }
    ]
  }
]